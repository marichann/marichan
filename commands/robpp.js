module.exports = {
    name: "robpp",
    category: "util",
    description: ["rob the profile picture", "ex:", "robpp @user"],
    help: "robpp [@user]",
    execute(p){
        const { msg } = p;
    }
}